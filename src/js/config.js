// after 2.2 seconds the painting canvas is cleared if there is no interaction.
export const PAINT_CLEAR_TIMEOUT = 2.2;

// after five idle minutes the entire widget is reload to its initial state.
export const IDLE_RELOAD = 300;

// wait 1.5 seconds to show the last train step when stopping training
export const LAST_TRAIN_STEP_TIMEOUT = 1.5;
