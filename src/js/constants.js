export const PAINT_CLEAR_TIMEOUT = 2.2; //after 2.2 seconds the painting canvas is cleared if there is no interaction.
export const IDLE_RELOAD = 300; //after five idle minutes the entire widget is reload to its initial state.
